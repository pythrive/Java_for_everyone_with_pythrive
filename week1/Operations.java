public class Operations {

    private static int a = 10;
    private static int b = 20;
    
   // ====== BASIC OPERATIONS ========

   /* 1.1 Subtraction 
    *   operand - operand = difference 
        private static int a_b = b - a ;
    */
    
    /* 1.2 Addition 
     *  operand + operand = sum 
     *  private static int add = b+a;
     */

     /* 1.3 Multiplecation
      *  operand * oparand = product 
      * private static int mult = b*a 
      */
    
    /* 1.4 Division
     *    operand / operand = qoutient 
          private static double qout = (double) b / a ;
     */
    
     /* 1.5 Modulus 
      *  operand % operand = remainder
         private static int rem = b % 15;
      */
    
    /* 1.6 Incrementing and Decrementing 
     *  int p = 10;
     *  p++; // p will be eleven
     * 
     * int q = 16 
     * q--;  // q will be now 15
     */

     /* 1.7 Unary Oprations
      *    private static int positiveValue = +5;
           private static int negativeValue = -5;
      */

      //  ======= RELATIONAL OPERATIONS ========

    /*  2.1 Equal to operation (==)
     *     private static boolean isEqual = (a == b); */

    /* 2.2 Not Equal to (!=)
     *     private static boolean isNotEqual = (a != b);
     */

    /* 2.3 Greater than (>)
     *     private static boolean isGreater = (b > a); // true 
     */
      
    /* 2.4 Less than (<)
     *     private static boolean isLess = (b < a); // false
     */


    /* 2.5 Grater than or equal to (>=)
     *     private static boolean geterEqual = (b >= a); // true
     */

    /* 2.6 Less or equal to (<=)
     *     private static boolean lessEqual = (b <= a); // false
     */

    // ======= LOGICAL OPERATIONS =========
            // all conditions must be correct to get a true
    /* 3.1 AND operator (&&)
     *   private static boolean result = (a == a)  && (b == b); // true
     *   private static boolean result_ = (a == b)  && (b == b); // false
     */

    /* 3.2 OR operator (||)                  
            // at least one condition must be correct to get a true
     *   private static boolean or_result = (a == a)  || (b == b); // true                                   
     *   private static boolean or_result_ = (a == b)  || (b == b); // true
     */


    /* 3.3 NOT operator (!)                       
     *   private static boolean not_result = !(a == b)  ; // true                                   
     *   private static boolean not_result_ = !(b == b) ; // false
     */

}



